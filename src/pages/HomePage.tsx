import React from 'react';
import { Helmet } from 'react-helmet';

const HomePage = () => {
   return (
      <div>
         <Helmet>
            <title>Trang chủ</title>
         </Helmet>
         HomePage
      </div>
   );
};

export default HomePage;
